# configs/

This directory is reserved for canonical YAML/config presets.

Intended contents include:

- strategy configs
- experiment presets
- validation presets
- workflow presets

Phase 0/1 note:

The repo may still rely on `examples/pipeline_queries/` and ad hoc script-local defaults.
Those are compatibility-era surfaces. New canonical reusable presets should converge here.

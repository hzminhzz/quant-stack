# legacy/validation_scripts/

This directory is the intended home for legacy validation entrypoints such as root
`validate_*.py` scripts.

During compatibility phases, old root paths may remain as wrappers or unchanged entrypoints.
Do not add new default validation workflows here.
